# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual Environment
venv/
.venv/ 
env/
ENV/

# IDE / Editor specific
.vscode/ 
# You might want to commit .vscode/settings.json if it contains project-specific settings 
# that are safe and useful for collaborators, but generally ignore the rest.
# If you use a .env file for secrets (see step below), add it here:
# .env 

# Secrets (though we are using environment variables for the key)
secrets.py
*.pem
*.key

# Operating system files
.DS_Store
Thumbs.db
